<a href="/clothing" class="home">
    Home
</a>
<div class="page">
<h1>Shop by Vendors</h1>
    {{#each users}}
        <a href="/users/{{_id}}">
            <h3>{{name}}</h3>
            <p>{{numberOfItems}} listings</p>
        </a>
    {{/each}}
</div>